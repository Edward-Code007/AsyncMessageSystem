name: .Net
run-name: Deploy by @ ${{github.actor}} by event ${{github.event_name}}
on: 
    workflow_dispatch:
    schedule:
      - cron: '00 00 * * 1-5'
jobs:
  Test: 
      uses: ./.github/workflows/test.yml
      with:
        path: ./AsyncMessage.Test/AsyncMessage.test.csproj
  
       


        
                


